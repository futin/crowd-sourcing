// core modules

// 3rd party modules

// internal alias modules

// internal modules
const addNominationValidators = require('./add-nomination-validators');
const updateNominationValidators = require('./update-nomination-validators');

module.exports = {
  addNominationValidators,
  updateNominationValidators,
};
